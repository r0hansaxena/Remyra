{
  "name": "remyra",
  "version": "1.0.0",
  "description": "Remyra — Cross-Chain Stablecoin Remittance Engine on Polkadot PVM",
  "scripts": {
    "compile": "npx hardhat compile",
    "test": "npx hardhat test",
    "deploy:local": "npx hardhat run scripts/deploy.ts --network localhost",
    "deploy:westend": "npx hardhat run scripts/deploy.ts --network westend",
    "node": "npx hardhat node"
  },
  "keywords": [
    "polkadot",
    "pvm",
    "remittance",
    "stablecoin",
    "xcm",
    "cross-chain"
  ],
  "license": "MIT",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@types/chai": "^4.3.20",
    "@types/mocha": "^10.0.10",
    "@types/node": "^25.3.3",
    "hardhat": "^2.19.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0",
    "dotenv": "^16.3.0"
  }
}
