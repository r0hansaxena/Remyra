{
  "name": "remyra",
  "version": "1.0.0",
  "description": "Remyra — Cross-Chain Stablecoin Remittance Engine on Polkadot PVM",
  "scripts": {
    "compile": "npx hardhat compile",
    "test": "npx hardhat test",
    "deploy:local": "npx hardhat run scripts/deploy.js --network localhost",
    "deploy:westend": "npx hardhat run scripts/deploy.js --network westend",
    "node": "npx hardhat node"
  },
  "keywords": [
    "polkadot",
    "pvm",
    "remittance",
    "stablecoin",
    "xcm",
    "cross-chain"
  ],
  "license": "MIT",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0",
    "dotenv": "^16.3.0"
  }
}